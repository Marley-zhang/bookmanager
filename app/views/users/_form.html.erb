<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <input type="radio" id="user_gender" name="user[gender]" VALUE="male" <%= checked="checked" if @user.gender == "male" %> >boy
    <input type="radio" id="user_gender" name="user[gender]" VALUE="male" <%= checked="checked" if @user.gender == "female" %> >gril
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
